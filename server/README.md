# Server Workspace

Hono API for machines, members, and sessions.

Required environment variables:

- `DATABASE_URL`
- `MACHINE_KEY_SECRET`

Run in development:

```bash
bun run dev
```

Drizzle commands:

```bash
bun run db:generate
bun run db:push
```
